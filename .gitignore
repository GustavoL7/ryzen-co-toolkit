# Baixados (ferramentas de terceiros — licencas nao permitem redistribuicao)
tools/*
!tools/.gitkeep

# Artefatos de runtime do dispatcher
cmd.txt
logs/*
!logs/.gitkeep

# Windows
Thumbs.db
desktop.ini

# opencode — config/estado LOCAL do PC (nunca commitar, nunca push ao GitHub)
opencode.json
opencode.jsonc
.opencode/spec_atual.md
.opencode/out/
.opencode/memory/*.local.md

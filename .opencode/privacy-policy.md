# Política de privacidade — consultores externos (treinam com dados)

Arquivo canônico por repo (vale só para este repo).

- `muse_agent: allow_all`
  Autorizado pelo dono em 2026-09-10: repo 100% open-source, sem segredos/credenciais/dados
  de clientes. Muse (e por analogia terra/gemini-sub) pode receber qualquer conteúdo deste repo.
  Segue valendo o aviso: nunca enviar `.env`/tokens (não existem neste repo).

Valores possíveis: `never` | `ask_per_task` | `allow_public` | `allow_all`
